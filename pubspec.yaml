name: flutter_quiz_advanced
description: Application de quiz Flutter avec Provider et BLoC
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Gestion d'état
  provider: ^6.1.1
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  shared_preferences: ^2.2.2

  # Utilitaires
  http: ^1.1.0  # Optionnel pour les images réseau

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # Supprimez ou commentez les assets qui n'existent pas
  # assets:
  #   - assets/images/
  #   - assets/data/

  # Supprimez ou commentez les fonts qui n'existent pas
  # fonts:
  #   - family: Montserrat
  #     fonts:
  #       - asset: assets/fonts/Montserrat-Regular.ttf
  #       - asset: assets/fonts/Montserrat-Bold.ttf
  #         weight: 700